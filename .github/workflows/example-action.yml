name: Example Workflow

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: 22f3001851@ds.study.iitm.ac.in
        run: echo "Hello, world!"
